kind: Deployment
apiVersion: apps/v1
metadata:
  name: trinitycore-bnetserver
  namespace: trinitycore-master
  labels:
    app.kubernetes.io/part-of: trinitycore
spec:
  replicas: 0
  selector:
    matchLabels:
      app: trinitycore-bnetserver
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: trinitycore-bnetserver
    spec:
      volumes:
        - name: trinityconfig
          configMap:
            name: trinitycore-config
            defaultMode: 420
      containers:
        - resources: {}
          terminationMessagePath: /dev/termination-log
          name: bnetserver
          command:
            - /opt/trinitycore/bin/bnetserver
          ports:
            - containerPort: 1119
              protocol: TCP
          imagePullPolicy: Always
          volumeMounts:
            - name: trinityconfig
              mountPath: /opt/trinitycore/etc
          terminationMessagePolicy: File
          image: >-
            image-registry.openshift-image-registry.svc:5000/trinitycore-master/trinitycore:master
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: Recreate
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
